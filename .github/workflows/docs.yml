name: docs
on:
  push:
    branches: [master]
  pull_request:

permissions:
  contents: read
jobs:
  markdownlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
      - run: npx --yes markdownlint-cli2 "**/*.md"
  links:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "TODO: link check con lychee"'
